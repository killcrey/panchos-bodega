# Local env — every VITE_-prefixed value here is intentionally public
# (see CLAUDE.md's Deployment Protocol), but it shouldn't be tracked
# regardless; Netlify's own dashboard env vars are what the real build
# actually uses.
.env

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
